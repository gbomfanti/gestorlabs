/**
 * Gestor Labs UI Patterns - Entry Point
 * 
 * Responsabilidades:
 * - Reexportar todos os padrões
 * - Fornecer um ponto único de importação
 * - Exportar tipos TypeScript
 * 
 * Uso:
 * import { patterns, components, layout, responsive } from '@/lib/styles'
 * import type { UIPatterns } from '@/lib/styles'
 */

export * from './patterns';
export { default } from './patterns';